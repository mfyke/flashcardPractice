var word = require("./word.js");

var game = function() {
	//randomly selects a word and uses the word constructor to store it
	console.log(new word("tariq"));
	//inquires player to guess and keep track of remaining guesses
}

game();